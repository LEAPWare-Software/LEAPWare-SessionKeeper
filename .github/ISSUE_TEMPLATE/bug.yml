name: Bug report
description: Something in sessionkeeper behaves differently than documented.
labels: ["bug"]
body:
  - type: dropdown
    id: component
    attributes:
      label: Component
      options:
        - core (lws_core engine / rules)
        - adapters/claude
        - adapters/codex
        - plugins/claude/lws
        - plugins/codex/lws
        - scripts (build/validate/release)
        - docs
    validations:
      required: true
  - type: textarea
    id: what-happened
    attributes:
      label: What happened
      description: What did sessionkeeper do, and what did you expect instead?
    validations:
      required: true
  - type: textarea
    id: repro
    attributes:
      label: Reproduction steps
      description: Policy file used (or "default"), the exact event/prompt, and the command or hook invocation.
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: sessionkeeper version
    validations:
      required: true
  - type: input
    id: host
    attributes:
      label: Host and OS
      description: e.g. "Claude Code on Windows 11" or "Codex CLI on macOS"
    validations:
      required: true
