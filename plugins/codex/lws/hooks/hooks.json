{
  "description": "lws token-policy enforcement (mirrors the Claude Code plugin's hooks.json)",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${PLUGIN_ROOT}/bin/lws_hook.py\" || python \"${PLUGIN_ROOT}/bin/lws_hook.py\"",
            "commandWindows": "python \"${PLUGIN_ROOT}\\bin\\lws_hook.py\"",
            "timeout": 10,
            "statusMessage": "lws: checking dispatch against token policy"
          }
        ]
      }
    ]
  }
}
